<div class="modal fade" id="modalNewFolder" tabindex="-1" aria-labelledby="modalNewFolderLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modalNewFolderLabel">Nouveau dossier</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form name="folder_form" id="confirm_form" action="{{ path('newfolder',{'idPath':path_.id}) }}">
                    <p> Veuillez saisir le nom du dossier </p>

                    <br><label for="nomFolder"></label><input type="text" id="nomFolder" name="nomFolder" value="" required />

                    <button type="submit" id="submit_form" class="btn btn-primary">Valider</button>

                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <!--<button type="button" class="btn btn-primary">Save changes</button> -->
            </div>
        </div>
    </div>
</div>